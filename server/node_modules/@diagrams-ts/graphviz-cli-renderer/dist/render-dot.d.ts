export declare type CliRendererArgs = {
    format: string;
    outputFile: string;
    dotPath?: string;
};
export declare const CliRenderer: (initialArgs: CliRendererArgs) => (input: string) => Promise<string>;
export declare const renderDot: ({ format, outputFile, input, dotPath, }: {
    format: string;
    outputFile: string;
    input: string;
    dotPath?: string | undefined;
}) => Promise<string>;
